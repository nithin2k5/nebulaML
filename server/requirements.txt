# Every direct dependency carries an upper bound. Without one, a fresh install
# resolves to whatever is newest — this repo was pinned only at the floor, so a
# clean environment today pulls numpy 2.x, pandas 3.x, albumentations 2.x and
# opencv 5.x, none of which the training loops were tested against.
#
# Bounds sit at the next major of the line each dependency was written for.
# For byte-exact reproducibility (deploys, CI cache), generate a lock on top:
#   pip install -r requirements.txt && pip freeze > requirements.lock.txt

fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.23.0,<1.0.0
python-multipart>=0.0.6,<1.0.0
pydantic>=2.0.0,<3.0.0
pydantic[email]>=2.0.0,<3.0.0
email-validator>=2.0.0,<3.0.0
ultralytics>=8.2.0,<9.0.0
opencv-python>=4.8.0,<5.0.0
numpy>=1.24.0,<3.0.0
pillow>=10.0.0,<13.0.0
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
transformers>=4.40.0,<5.0.0
einops>=0.8.0,<1.0.0
timm>=1.0.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0
aiofiles>=23.0.0,<26.0.0
pyyaml>=6.0,<7.0
pandas>=2.0.0,<3.0.0
mysql-connector-python>=8.0.0,<27.0.0
PyJWT>=2.8.0,<3.0.0
bcrypt>=4.0.0,<6.0.0
fastapi-mail>=1.4.0,<2.0.0
pydantic-settings>=2.2.0,<3.0.0
albumentations>=1.3.1,<3.0.0
slowapi>=0.1.9,<1.0.0

# Development tools
ruff>=0.3.0,<1.0.0
black>=24.2.0,<26.0.0
pytest>=8.0.0,<10.0.0
pytest-asyncio>=0.23.0,<2.0.0
requests>=2.31.0,<3.0.0
iterative-stratification>=0.1.7,<1.0.0
ImageHash>=4.3.1,<5.0.0
